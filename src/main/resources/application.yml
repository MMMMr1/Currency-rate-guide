nbrb:
  url: https://api.nbrb.by

spring:
  datasource:
    url: jdbc:h2:file:/data/demo
    username: nbrb
    password:
    driver-class-name: org.h2.Driver

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: none
    show-sql: true
  h2:
    console:
      enabled: true

  mvc:
    format:
      date: yyyy-MM-dd

  liquibase:
    enabled: true